{%- comment -%}
  Kept so existing theme editor instances do not break.
  Purchase UI now lives in Product buy box to keep Add to cart on the page.
{%- endcomment -%}
<div class="offer-legacy" hidden></div>

{% schema %}
{
  "name": "Subscription options",
  "enabled_on": {
    "templates": ["product"]
  },
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Showerhead product"
    },
    {
      "type": "product",
      "id": "filter_product",
      "label": "Carbon filter product"
    }
  ],
  "blocks": [
    {
      "type": "@app"
    }
  ],
  "presets": [{ "name": "Subscription options" }]
}
{% endschema %}
