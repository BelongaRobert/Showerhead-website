{%- comment -%}
  Shared product URL for every Shop / Shop now CTA.
  Always prefers the main showerhead — never the Carbon Filter refill SKU.
{%- endcomment -%}
{%- render 'resolve-shop-product', product: product -%}
